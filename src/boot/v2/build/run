echo "NASM Building..."
nasm -f elf32 ../boot.asm -o kasm.o
echo "GCC Building..."
gcc -m32 -Wall -c ../kernel.c
echo "Linking..."
ld -m elf_i386 -T ../link.ld -o kernel kasm.o kernel.o
echo "Emulating.."
qemu-system-i386 -kernel kernel

