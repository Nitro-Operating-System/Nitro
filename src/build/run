echo "NASM Building..."
nasm -f elf32 ../sys/boot.asm -o kasm.o
echo "GCC Building..."
gcc -m32 -Wall -c ../kernel/kernel.c ../kernel/external.c

echo "Linking..."
ld -m elf_i386 -T ../sys/link.ld -o kernel kasm.o kernel.o
echo "Emulating.."
qemu-system-i386 -kernel kernel

